My Portfolio

## Visitor Counter

The visitor counter is backed by Upstash Redis in production. Configure these environment variables before deploying:

```bash
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
```

Local development uses `.visitor-counter.json`, which is ignored by git.
